1. Real-world objects contain STATE and BEHAVIOR
2. A software object's state is stored in FIELDs
3. A software object's behavior is exposed through METHODs
4. Hiding internal data from the outside world, and accessing it only through publicly exposed methods is knows as data "ENCAPSULATION"
5. A blueprint for a software object is called a CLASS
FAIL =>> 6. Common behavior can be define in a SUPER_CLASS and inherited into a SUB_CLASS using the EXTENDS keyword
7. A collection of methods with no implementation is called an INTERFACE
8. A namespace that organizers classes and interfaces by functionality is called a PACKAGE
9. The term API stand for APPLICATION PROGRAMMING INTERFACE